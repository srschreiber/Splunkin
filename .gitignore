/build/
/third_party/install/
/third_party/glad/
/third_party/*.h
# vendored submodule sources are tracked via .gitmodules, their build artifacts are not
__pycache__/
*.o
.DS_Store

# Piper TTS engine + voice are provisioned by scripts/setup_piper.sh, not committed.
assets/piper/
